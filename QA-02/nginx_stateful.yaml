# ---
# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: standard
# provisioner: kubernetes.io/no-provisioner
# volumeBindingMode: Immediate

# ---
# apiVersion: storage.k8s.io/v1
# kind: StorageClass
# metadata:
#   name: standard
#   annotations:
#     storageclass.kubernetes.io/is-default-class: "true"  # Sets this as the default StorageClass
# provisioner: rancher.io/local-path
# volumeBindingMode: WaitForFirstConsumer
# reclaimPolicy: Delete
# allowVolumeExpansion: false


---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nginx-storage-pv
spec:
  capacity:
    storage: 8Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: standard
  local:
    path: /mnt/nginx-storage
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: Exists
  # hostPath:
  #   path: "/tmp/nginx-storage"    
  # nodeAffinity:
  #   required:
  #     nodeSelectorTerms:
  #       - matchExpressions:
  #           - key: kubernetes.io/hostname
  #             operator: In
  #             values:
  #               - secure-nginx-cluster-worker  # Add your worker node name here
  #               - kind-control-plane           # Or remove this line if you don't want to restrict to control-plane

  # nodeAffinity:
  #   required:
  #     nodeSelectorTerms:
  #       - matchExpressions:
  #           - key: kubernetes.io/hostname
  #             operator: In
  #             values:
  #               - kind-control-plane   



---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-storage
  namespace: nginx-namespace
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nginx-statefulset
  namespace: nginx-namespace
spec:
  serviceName: headless-svc        # Ties the StatefulSet to the headless service
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: ghcr.io/mohtashims/secure-nginx:1.19-alpine69952c963096e214fbbeda6b583e6c23e085a509
          #imagePullPolicy: Never
          ports:
            - containerPort: 80
          securityContext:
            runAsUser: 1003
            runAsGroup: 1003
            capabilities:
              add: ["NET_BIND_SERVICE"]
              drop: ["ALL"]
            readOnlyRootFilesystem: false
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
              #scheme: HTTP
            timeoutSeconds: 2
            initialDelaySeconds: 30
            periodSeconds: 5
            failureThreshold: 3
 
          readinessProbe:
            httpGet:
              path: /
              port: 80
              #scheme: HTTP
            timeoutSeconds: 2  
            initialDelaySeconds: 25
            periodSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: nginx-html
              mountPath: /usr/share/nginx/html  # Mounting the volume here
      volumes:
        - name: nginx-html
          persistentVolumeClaim:
            claimName: nginx-storage
            
      imagePullSecrets:
        - name: github-container-registry        
---
apiVersion: v1
kind: Service
metadata:
  name: headless-svc
  namespace: nginx-namespace
spec:
  clusterIP: None                  # Defines the service as headless
  selector:
    app: nginx
  ports:
    - name: https
      protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-loadbalancer
  namespace: nginx-namespace
spec:
  type: LoadBalancer               # Exposes the service externally
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
